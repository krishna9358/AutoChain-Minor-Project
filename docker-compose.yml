services:
  # ─── Database ───────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: autochain-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: autochain
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: autochain_dev
      POSTGRES_INITDB_ARGS: "-E UTF8 --locale=C"
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./scripts/init-db.sh:/docker-entrypoint-initdb.d/init-db.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d autochain"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    networks:
      - autochain-network

  # ─── Backend API ────────────────────────────────────────────────────────
  backend:
    build:
      context: ./primary-backend
      dockerfile: Dockerfile.dev
    container_name: autochain-backend
    restart: unless-stopped
    env_file:
      - ./primary-backend/.env
    environment:
      DATABASE_URL: postgresql://postgres:autochain_dev@postgres:5432/autochain
      JWT_SECRET: autochain-dev-secret-key
      PORT: "3001"
      NODE_ENV: development
      ENCRYPTION_KEY: autochain-dev-encryption-key-32ch
    ports:
      - "3001:3001"
    volumes:
      - ./primary-backend/src:/app/src
      - ./primary-backend/prisma:/app/prisma
      - ./primary-backend/tsconfig.json:/app/tsconfig.json
      - backend_node_modules:/app/node_modules
      - backend_dist:/app/dist
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3001/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - autochain-network

  # ─── Frontend (Next.js Dev) ─────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: autochain-frontend
    restart: unless-stopped
    env_file:
      - ./frontend/.env.local
    environment:
      NEXT_PUBLIC_BACKEND_URL: http://localhost:3001
      NEXT_PUBLIC_DEV_MODE: "true"
      NEXT_PUBLIC_DEV_TOKEN: dev-demo-token
      NODE_ENV: development
    ports:
      - "3000:3000"
    volumes:
      - ./frontend/app:/app/app
      - ./frontend/components:/app/components
      - ./frontend/lib:/app/lib
      - ./frontend/store:/app/store
      - ./frontend/hooks:/app/hooks
      - ./frontend/public:/app/public
      - frontend_next:/app/.next
      - frontend_node_modules:/app/node_modules
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - autochain-network

volumes:
  pgdata:
    driver: local
  backend_node_modules:
    driver: local
  backend_dist:
    driver: local
  frontend_node_modules:
    driver: local
  frontend_next:
    driver: local

networks:
  autochain-network:
    driver: bridge
