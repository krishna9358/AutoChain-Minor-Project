FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npx prisma generate 2>/dev/null || true
EXPOSE 3002
CMD ["sh", "-c", "npx prisma generate 2>/dev/null; npm run dev"]
