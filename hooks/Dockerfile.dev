FROM node:20-slim
RUN apt-get update -y && apt-get install -y openssl wget
WORKDIR /app
COPY package*.json ./
RUN npm install --production=false && npm list typescript
COPY . .
RUN npx prisma generate 2>/dev/null || true
EXPOSE 3002
CMD ["sh", "-c", "npx prisma generate 2>/dev/null; npm run dev"]
